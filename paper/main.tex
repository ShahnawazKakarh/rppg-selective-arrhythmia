% rppg-selective-arrhythmia — IEEE J-BHI draft
%
% Compile:  pdflatex main.tex; bibtex main; pdflatex main.tex; pdflatex main.tex
% Requires: texlive-publishers (for IEEEtran), texlive-bibtex-extra
%
% Class:    IEEEtran journal (single-column draft mode for first reviews;
%           switch to default 2-column for submission by removing the
%           [draftcls,onecolumn] options).

\documentclass[journal]{IEEEtran}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{cite}
\usepackage[hidelinks]{hyperref}
\usepackage{url}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

\title{Learned Class-Conditional Signal-Quality Deferral for Selective rPPG-Based Atrial Fibrillation Screening}

\author{Muhammad~Shahnawaz~Khan%
\thanks{M. S. Khan is an independent researcher, Lahore, Pakistan
        (e-mail: shahnawaz.jrw@gmail.com; ORCID: 0009-0007-4055-6563).}%
\thanks{Manuscript drafted June 2026.}}

\markboth{IEEE Journal of Biomedical and Health Informatics, Submission Draft}%
{Khan: Learned Class-Conditional Signal-Quality Deferral for rPPG-AF Screening}

\maketitle

\begin{abstract}
Contactless atrial fibrillation (AF) screening from facial-video remote photoplethysmography (rPPG) is an attractive opportunistic-screening modality, but deployment requires the model to abstain on segments it cannot classify safely. Standard selective-prediction methods rank samples by model confidence alone, ignoring the physical signal-quality information available from the rPPG pulse waveform itself. We study post-hoc deferral policies that combine model confidence with spectral signal-to-noise ratio (SNR) in the cardiac band. We first show that a na\"{i}ve linear combination achieves an apparent 18\,\% relative improvement in area under the risk-coverage curve (AURC) over uncertainty-only deferral on a 45{,}064-segment synthetic-rPPG benchmark derived from the PhysioNet/CinC 2017 AF Challenge --- but this aggregate gain is clinically harmful: AF recall at 50\,\% coverage collapses from 0.71 to 0.04 because AF's irregular rhythm is itself a low-SNR signature in the cardiac band, so the deferral rule systematically rejects positives. We then introduce \textbf{Learned Class-Conditional Signal-Quality Deferral (LW-CCSD)}, a constrained-grid-search procedure that estimates per-predicted-class quality weights on validation data subject to a configurable AF-recall floor. LW-CCSD makes the safety / accuracy trade-off explicit and tunable: across the Pareto frontier we measure +4.1\,\% AURC improvement at $\leq 0.01$ AF-recall cost, scaling to +15.6\,\% AURC at 8 percentage-point AF cost. The method generalises across \textbf{five} uncertainty quantification methods (deterministic single-pass softmax, MC Dropout, deep ensembles, Evidential Deep Learning, SNGP) and is extended with a full conformal-coverage arc (per-class, Bonferroni, Holm step-down, and multivariate-beta exact-independent joint procedures at family-wise $\alpha=0.10$). Across the UQ benchmark the strongest configuration is a 5-member EDL ensemble (test AURC 0.1640, accuracy 0.747). We further document a novel mechanism finding: EDL with KL annealing absorbs signal-quality information \emph{in-method}, making LW-CCSD redundant on top of EDL --- the no-margin property is established at five independent levels of robustness, including a KL$\to\infty$ ablation that refines the mechanism to a controllable training-time consequence of the KL warm-up schedule rather than an architectural property of the Dirichlet head. Strikingly, LW-CCSD applied to a single-forward-pass classifier achieves better selective performance than a five-model deep ensemble without LW-CCSD, suggesting the method can substitute for compute-expensive ensembling in deployed clinical screens.
\end{abstract}

\begin{IEEEkeywords}
Remote photoplethysmography (rPPG), atrial fibrillation, selective prediction, uncertainty quantification, conformal prediction, calibration, contactless cardiac screening, signal quality, deferral policies.
\end{IEEEkeywords}

% =============================================================================
\section{Introduction}
% =============================================================================
\IEEEPARstart{A}{trial} fibrillation (AF) is the most common sustained cardiac arrhythmia and a major risk factor for stroke. It is frequently asymptomatic and paroxysmal, so opportunistic screening outside clinical settings has high public-health value. Remote photoplethysmography (rPPG), which recovers a pulse waveform from skin-colour variation in face video, has emerged as a promising contactless modality. Recent studies report high accuracy for rPPG-based AF detection. However, none report risk-coverage curves, calibration error, or principled deferral policies. rPPG signal quality varies sharply with motion, lighting, skin tone, frame rate, and camera quality, so a confident prediction on a low-quality signal is not deployable.

This work formalises rPPG-based AF screening as a \emph{selective-prediction} problem: a system that defers cases it cannot classify safely. We make five contributions:

\begin{enumerate}
\item We benchmark \textbf{five} uncertainty-quantification (UQ) methods --- deterministic single-pass softmax, MC Dropout, deep ensembles, Evidential Deep Learning (EDL), and Spectrally-Normalised Gaussian Process (SNGP) classifiers --- on a 45{,}064-segment synthetic-rPPG substrate derived from the PhysioNet/CinC 2017 AF Challenge. The strongest configuration is a 5-member EDL ensemble (test AURC 0.1640, accuracy 0.747); SNGP wins calibration (ECE 0.029).

\item We identify and explain a clinically-harmful failure mode in na\"{i}ve signal-quality-aware deferral: combining model confidence with spectral SNR boosts aggregate AURC by 18\,\% but collapses AF recall from 0.71 to 0.04 at 50\,\% coverage, because AF's irregular rhythm is itself a low-SNR signature.

\item We introduce \textbf{Learned Class-Conditional Signal-Quality Deferral (LW-CCSD)}, a post-hoc, model-agnostic procedure that learns per-predicted-class quality weights subject to a configurable AF-recall floor. LW-CCSD makes the safety / accuracy trade-off explicit and tunable, recovering 80\,\% of the na\"{i}ve aggregate gain while preserving AF recall.

\item We extend LW-CCSD with a full \textbf{conformal-coverage arc} at family-wise $\alpha=0.10$ comprising four procedures: per-class Clopper--Pearson lower bounds, Bonferroni union-bound joint coverage, Holm step-down joint coverage, and a multivariate-beta exact-independent joint bound. The arc shows that the 1.75\,\% rel AURC gap left by joint-coverage methods relative to per-class is intrinsic to the joint-coverage objective rather than Bonferroni's union-bound conservativeness.

\item We document a novel \textbf{EDL mechanism finding}: EDL with KL annealing absorbs signal-quality information in-method, making LW-CCSD redundant on top of EDL. The no-LW-CCSD-margin property is established at five independent levels of robustness (empirical optimisation, variance across initialisation seeds, conformal coverage at $\alpha=0.10$, KL annealing schedule among annealed regimes, and a KL$\to\infty$ ablation that refutes the property in the no-annealing limit). The KL$\to\infty$ ablation refines the mechanism: the no-margin property is a controllable training-time consequence of the KL warm-up schedule, not an architectural property of the Dirichlet evidence head.
\end{enumerate}

A deployment-relevant corollary: LW-CCSD applied to a single-pass deterministic classifier outperforms an unaugmented 5-model deep ensemble on the same selective-prediction objective. Lightweight inference plus the free post-hoc deferral rule can replace compute-expensive ensembling.

% =============================================================================
\section{Related Work}
% =============================================================================
\subsection{rPPG-Based AF Detection}
Remote photoplethysmography (rPPG) recovers the cardiac pulse waveform from periodic colour modulations of facial skin in standard RGB video. Classical extractors --- CHROM~\cite{dehaan2013chrom} and POS~\cite{wang2017pos} --- combine the colour channels by a fixed projection designed to suppress motion-correlated noise. Learned end-to-end extractors --- PhysNet~\cite{yu2019physnet} and successors --- replace this projection with a CNN trained on paired video / contact-PPG corpora and substantially close the gap to clinical-grade sensors under cooperative conditions.

AF detection from rPPG has been demonstrated on dedicated corpora. The OBF database~\cite{li2018obf} contains 200 subjects, of whom 100 are paroxysmal AF patients recorded both pre- and post-cardioversion; this remains the standard benchmark for face-video AF screening but is gated and not in general circulation. Yan~\emph{et~al.}~\cite{yan2022consumer} reported high accuracy for an AF screening pipeline using consumer-grade smartphone cameras on 217 hospitalised subjects. Reported metrics in both lines of work are dominated by point accuracy and ROC-AUC; we are not aware of any rPPG-AF study that publishes risk-coverage curves, calibration error, or principled deferral policies on inputs that are ambiguous due to motion, occlusion, or low signal quality. This omission is the gap the present work addresses.

\subsection{Uncertainty Quantification for Medical AI}
Uncertainty quantification (UQ) in deep neural networks for medical imaging and physiology has converged on a small set of practical methods. Monte-Carlo Dropout~\cite{galghahramani2016dropout} treats inference-time dropout as approximate variational inference over the weight posterior, yielding predictive uncertainty at the cost of $T$ stochastic forward passes. Deep Ensembles~\cite{lakshminarayanan2017ensembles} aggregate $M$ independently trained models and remain competitive with more elaborate Bayesian approaches in calibration and out-of-distribution detection. Evidential Deep Learning~\cite{sensoy2018evidential} reframes classification as inferring the parameters of a Dirichlet over the simplex, separating data uncertainty from epistemic uncertainty in a single forward pass. SNGP~\cite{liu2020sngp} achieves distance-aware uncertainty by combining a spectrally-normalised feature extractor with a Gaussian-process classifier head. Split conformal prediction~\cite{angelopoulos2021conformal} provides distribution-free finite-sample coverage guarantees by calibrating a non-conformity threshold on a held-out set.

Selective classification~\cite{geifman2017selective} couples a base classifier with an abstention head, traded off against a target coverage. Calibration is typically reported via the Expected Calibration Error~\cite{naeini2015ece} or the Brier score. None of these methods individually incorporate physical signal-quality information, which is the augmentation we study here.

\subsection{Signal-Quality-Aware Deferral}
In the PPG and rPPG literature, signal-quality indices (SQIs) are conventionally used as a \emph{hard gate}: segments with SQI below a threshold are dropped before classification, and only the remainder is scored. This approach has two limitations. First, it conflates ``too low to use'' with ``low but classifiable with appropriate caution''; a continuous quality score discarded at the gate cannot be re-introduced as a calibration signal downstream. Second, the gate threshold is set without reference to the classifier's own uncertainty, so high-confidence predictions on borderline signals are over-rejected and low-confidence predictions on clean signals are under-rejected.

Class-conditional selective prediction has been studied in ordinal medical classification by the present author in the context of diabetic-retinopathy grading, where ordinal proximity of misclassification cost motivates per-class confidence thresholds. The present work extends the class-conditional principle in a new direction: rather than calibrating against misclassification cost, we combine model confidence with an auxiliary \emph{physical} sensor-quality feature, learn the mixing weight per predicted class, and constrain the optimisation against a per-class recall floor. To the best of our knowledge this combination --- post-hoc class-conditional auxiliary-feature deferral with formal recall-floor constraints --- has not previously appeared in either the rPPG, the cardiac-classification, or the selective-prediction literatures.

% =============================================================================
\section{Methods}
% =============================================================================
\subsection{Synthetic rPPG from ECG}
Real paired face-video / AF-label corpora are scarce. To enable controlled methodological experiments at scale, we synthesise rPPG-style pulse waveforms from publicly available single-lead ECG recordings. The synthesis preserves the rhythm component that AF classifiers ultimately rely on while abstracting away rPPG-specific morphological details (dicrotic notch, beat-to-beat amplitude variation tied to skin perfusion) that are also poorly recovered by camera-based rPPG.

Given an ECG signal at sampling rate $f_s^{\text{ECG}}$, we detect R-peaks $\{t_k\}$ with NeuroKit2's Pan--Tompkins-derived detector. For each R-peak we place a beat template at lag $\tau_{\text{PTT}} = 0.20\,\text{s}$ representing the pulse-transit-time delay between cardiac depolarisation and the systolic peak in the periphery. The beat template is an asymmetric Gaussian with peak position $\rho = 0.4$ along the beat duration $T_k = \min(t_{k+1} - t_k,\ 0.5 f_s^{\text{ECG}})$:
\begin{equation}
g(s; \rho, T_k) = \exp\!\left(-\frac{1}{2}\,\frac{(s - \rho T_k)^2}{\sigma(s)^2}\right),\quad s \in [0, T_k],
\end{equation}
with $\sigma(s) = \rho T_k / 2.5$ for $s \leq \rho T_k$ and $\sigma(s) = (1-\rho) T_k / 2.5$ otherwise (giving a short rising edge and a longer falling edge, matching the dominant systolic morphology recovered by rPPG). Each placed beat is scaled by $1 + 0.05\,\epsilon_k$, $\epsilon_k \sim \mathcal{N}(0,1)$.

After beat placement the signal is corrupted with respiratory baseline wander $0.15 \sin(2\pi f_b t)$ at $f_b = 0.15$\,Hz, optionally with motion bursts and 4\,Hz lighting flicker (both disabled in the main experiments to keep the controlled setting clean), then resampled from $f_s^{\text{ECG}}$ to the target camera frame rate $f_s^{\text{rPPG}} = 30$\,Hz by rational polyphase resampling. Post-resample additive Gaussian noise with $\sigma = 0.05$ models residual camera and quantisation noise. The waveform is finally z-score normalised.

Labels are inherited at the record level for the PhysioNet/CinC 2017 substrate (each record receives a single label from REFERENCE.csv: N $\to$ NSR, A $\to$ AF, O $\to$ Other; noisy records labelled $\sim$ are dropped before training). For MIT-BIH the per-rhythm annotations are interpolated onto the 30\,Hz target rate to attribute each 10-s window to the dominant rhythm annotation in that window.

\subsection{Classifier and Uncertainty Heads}
The base classifier is a 1D-CNN $\to$ Transformer encoder operating on 10-second (300-sample at 30\,Hz) windows. The CNN stem has three convolutional blocks with output channels (32, 64, 128), each block consisting of a 1D convolution, batch normalisation, ReLU, and stride-2 max-pooling. The Transformer encoder operates over the resulting (channels, length) sequence with 2 layers, 4 attention heads, and a feed-forward width of 256. Mean-pooling across the sequence axis feeds a linear classification head over the three classes. Dropout at probability $p = 0.4$ is inserted after each convolutional block and inside the Transformer feed-forward sublayers; this is the source of the stochasticity used by MC Dropout inference.

Training minimises class-weighted cross-entropy with weights inversely proportional to class frequency (CinC: $\mathbf{w}_{\text{CE}} = (0.56,\ 3.79,\ 1.05)$). The optimiser is AdamW (learning rate $3\times10^{-4}$, weight decay $10^{-4}$) with cosine schedule and early stopping (patience 5 epochs) on validation macro-F1.

Three UQ heads are evaluated against the same base classifier without architectural change:
\begin{itemize}
\item \emph{Monte-Carlo Dropout} keeps dropout active at inference and averages softmax probabilities over $T = 30$ stochastic forward passes; predictive entropy serves as the confidence rank.
\item \emph{Deep Ensembles} train $M = 5$ independent models with different initialisation seeds and average their softmax outputs; the maximum averaged class probability is used as confidence.
\item \emph{Split Conformal Prediction} calibrates a non-conformity threshold $\hat{q}_\alpha$ on the validation set at miscoverage $\alpha = 0.1$; the deterministic max-softmax probability is used as the continuous confidence ranking for selective evaluation, while the conformal set sizes are reported separately.
\end{itemize}

Two additional heads modify the loss or output layer of the base classifier and are trained from scratch.

\paragraph{Evidential Deep Learning (EDL)} The softmax head is replaced by an evidential head that outputs non-negative evidence $\boldsymbol{e}(x) \in \mathbb{R}_{\geq 0}^{C}$ via a softplus activation. The Dirichlet concentration vector is $\boldsymbol{\alpha}(x) = \boldsymbol{e}(x) + 1$, with strength $S(x) = \sum_c \alpha_c(x)$ and mean class probability $\hat{p}_c(x) = \alpha_c(x) / S(x)$. The training objective is the Type-II maximum-likelihood loss of~\cite{sensoy2018evidential}:
\begin{equation}
\mathcal{L}_{\text{EDL}} = \sum_c (y_c - \hat{p}_c)^2 + \frac{\hat{p}_c (1 - \hat{p}_c)}{S + 1} + \lambda_t \cdot \mathrm{KL}\big[\mathrm{Dir}(\tilde{\boldsymbol{\alpha}}) \,\|\, \mathrm{Dir}(\mathbf{1})\big],
\end{equation}
where $\tilde{\boldsymbol{\alpha}}$ is $\boldsymbol{\alpha}$ with the ground-truth-class concentration set to $1$ (so the KL penalty only fires on incorrect-class evidence), and $\lambda_t$ is a KL annealing weight that ramps linearly from $0$ to $1$ over a configurable number of warm-up epochs. The standard configuration uses a $10$-epoch warm-up; we sweep this schedule in Section~\ref{sec:edl_kl_sensitivity}. At inference, the predictive uncertainty $u(x) = C / S(x) \in (0, 1]$ ranks the confidence; lower $u$ corresponds to higher confidence. We also report a $5$-member \emph{EDL ensemble} configuration that averages the predictive probabilities $\hat{p}_c$ across independently-trained EDL models (Section~\ref{sec:edl_ensemble}).

\paragraph{SNGP (Spectrally-Normalised Gaussian Process)} We follow~\cite{liu2020sngp}: spectral normalisation is applied to every linear and convolutional layer of the encoder to bound the feature extractor's Lipschitz constant, and the classification head is replaced by a Gaussian-process layer with $1024$ random Fourier features and a Laplace-approximation posterior over the GP weights. At inference, the predictive mean is mean-field corrected: $\hat{p}_c(x) \propto \sigma\big(\mu_c(x) / \sqrt{1 + \pi \sigma^2_c(x) / 8}\big)$, where $\mu_c, \sigma^2_c$ are the GP-output mean and variance. The maximum mean-field-corrected probability is used as confidence. SNGP gives distance-aware uncertainty without the architectural cost of an ensemble or the loss-modification of EDL.

\subsection{Conformal Joint-Coverage Procedures}
\label{sec:conformal_procedures}
For a deployed selective classifier with per-class recall floors $\{f_c\}_{c=1}^C$, the LW-CCSD optimiser of Eq.~(7) requires that each per-class recall on validation data satisfies $\widehat{\mathrm{recall}}^{(c)}_{\text{val}} \geq f_c$. To convert this empirical condition into a finite-sample distribution-free coverage guarantee, we replace each per-class empirical recall with its Clopper--Pearson lower confidence bound (LCB) at per-class miscoverage $\alpha_c$. The constraint becomes $\mathrm{LCB}_c^{(1 - \alpha_c)} \geq f_c$, and the question is how to set $\{\alpha_c\}$ for a desired family-wise guarantee $\Pr[\forall c: \widehat{\mathrm{recall}}^{(c)} \geq f_c] \geq 1 - \alpha$.

We evaluate four procedures spanning the natural design space at a fixed family-wise $\alpha = 0.10$:
\begin{itemize}
\item \emph{Per-class}: $\alpha_c = \alpha$ for each class. Gives a per-class $(1 - \alpha)$-LCB but does not control family-wise coverage; loosest constraint, largest LW-CCSD margin.
\item \emph{Bonferroni joint}: $\alpha_c = \alpha / C$. Controls family-wise coverage by the union bound: $\Pr[\forall c: \widehat{\mathrm{recall}}^{(c)} \geq f_c] \geq 1 - \sum_c \alpha_c = 1 - \alpha$. Conservative under independence; tightest constraint, smallest LW-CCSD margin.
\item \emph{Holm step-down joint}: Sort the per-class binomial tests by p-value $p_{(1)} \leq p_{(2)} \leq \dots \leq p_{(C)}$, and accept the $k$-th hypothesis (the $k$-th class's recall meets its floor) if $p_{(k)} \leq \alpha / (C - k + 1)$. Strictly more powerful than Bonferroni at the same family-wise level; recovers some margin between per-class and Bonferroni.
\item \emph{Multivariate-beta exact-independent joint}: Under the assumption that per-class binomial tests are independent --- which holds in our setup because val records are pre-partitioned by class label and each test is computed on disjoint trial counts --- the joint coverage factorises: $\Pr[\forall c: \widehat{\mathrm{recall}}^{(c)} \geq \mathrm{LCB}_c] = \prod_c \Pr[\widehat{\mathrm{recall}}^{(c)} \geq \mathrm{LCB}_c]$. Setting each per-class probability to $(1 - \alpha)^{1/C}$ gives the family-wise guarantee \emph{exactly}. The per-class $\alpha$ is therefore $\alpha_c = 1 - (1 - \alpha)^{1/C}$. For $C = 3$, $\alpha = 0.10$ this gives $\alpha_c = 0.0345$ vs Bonferroni's $\alpha_c = 0.0333$ --- the multivariate-beta bound is slightly more lenient than Bonferroni's union bound.
\end{itemize}

All four procedures plug into the same LW-CCSD constrained optimiser (Eq.~(7)) by replacing the empirical-recall constraint with the appropriate LCB constraint; only the LCB tightness changes.

\subsection{Selective Evaluation}
Let $\hat{p}(x)$ denote the confidence score assigned to input $x$ by a UQ method, with higher values corresponding to higher predicted reliability. For a chosen coverage $c \in (0, 1]$, the selective classifier retains the top-$\lceil cn \rceil$ predictions ranked by $\hat{p}(x)$ on a test set of $n$ samples and abstains on the rest. We report:
\begin{itemize}
\item \emph{Selective accuracy at coverage} $c$: classification accuracy on the retained subset.
\item \emph{Risk-coverage curve}: selective error $1 - \text{acc}$ plotted against $c$.
\item \emph{Area under the risk-coverage curve} (AURC, lower is better) integrated over $c \in [0, 1]$ via trapezoidal quadrature on the empirical curve.
\item \emph{Expected Calibration Error}~\cite{naeini2015ece} with 15 confidence bins.
\item \emph{Multi-class Brier score} on the predicted probability vector.
\item \emph{Per-class recall at coverage}, defined as $\#\{\text{correct kept predictions of class } c\}\,/\,\#\{\text{class } c \text{ in test}\}$, so deferred samples count as missed.
\end{itemize}
The per-class recall metric is what makes the AF-collapse failure mode of na\"{i}ve SQI deferral visible; aggregate accuracy or AURC alone hide it.

\subsection{Signal-Quality Features}
Two signal-quality features are computed per 10-second window from the 30\,Hz pulse waveform after bandpass filtering to the cardiac band $[0.7, 3.0]$\,Hz:
\begin{itemize}
\item \emph{Spectral SNR} (dB): $10 \log_{10} (P_{\text{in}} / P_{\text{out}})$ where $P_{\text{in}}$ is the integrated periodogram power in $[0.7, 3.0]$\,Hz and $P_{\text{out}}$ is the power in $[3.0, f_s/2]$\,Hz. Regular rhythm concentrates power near the fundamental heart-rate frequency, raising SNR; irregular rhythm distributes power across the cardiac band, lowering it.
\item \emph{Template SQI}: mean Pearson correlation between each detected beat and the median template across the window. Robust to amplitude variation but sensitive to overall waveform morphology.
\end{itemize}
Both features are computed deterministically at inference time without retraining and without access to ground-truth labels.

\subsection{Class-Conditional Deferral Policies}
We compare four scoring functions for selective prediction. Let $\hat{p}(x)$ denote model confidence, $q(x)$ the signal-quality feature (rank-normalised within the test set), and $\hat{c}(x)$ the predicted class.

\begin{align}
\text{UQ-only:}\quad & s_0(x) = \text{rank}(\hat{p}(x)) \\
\text{Na\"{i}ve SQI:}\quad & s_w(x) = (1-w)\,\text{rank}(\hat{p}(x)) + w\,\text{rank}(q(x)) \\
\text{AF-immune:}\quad & s_w^{\text{AI}}(x) =
  \begin{cases}
    \text{rank}(\hat{p}(x)) & \hat{c}(x) = \text{AF} \\
    s_w(x) & \text{otherwise}
  \end{cases} \\
\text{LW-CCSD:}\quad & s_{\mathbf{w}}^{\text{LW}}(x) = (1-w_{\hat{c}(x)})\,\text{rank}(\hat{p}(x)) + w_{\hat{c}(x)}\,\text{rank}(q(x))
\end{align}

In LW-CCSD, $\mathbf{w} = (w_{\text{NSR}}, w_{\text{AF}}, w_{\text{Other}})$ is learned by constrained grid search on the validation set:
\begin{align}
\mathbf{w}^{*} = \arg\min_{\mathbf{w} \in [0,1]^3}\ &\text{AURC}_{\text{val}}(s_{\mathbf{w}}^{\text{LW}}) \\
\text{s.t.}\quad & \text{recall}_{\text{val}}^{(c)}(s_{\mathbf{w}}^{\text{LW}}; c_0) \geq f_c\ \forall c
\end{align}
where $\text{recall}_{\text{val}}^{(c)}(s; c_0)$ denotes class-$c$ recall on val at coverage $c_0$, and $f_c$ is the per-class recall floor.

% =============================================================================
\section{Experimental Setup}
% =============================================================================
\subsection{Datasets}
We evaluate on two single-lead ECG corpora, both rendered as synthetic rPPG via the pipeline above:

\paragraph{PhysioNet/CinC 2017 AF Challenge~\cite{clifford2017cinc}.} The training portion of the challenge release contains 8{,}528 recordings between 9 and 60 seconds at 300\,Hz, labelled N (5{,}050), A (738), O (2{,}456), or $\sim$ (284). We drop the 284 noisy recordings and synthesise rPPG from the remaining 8{,}244. Records are stratified by class and split 70/15/15 at the record level (subject-disjoint by construction since each recording is from a distinct subject). Windowing at 10\,s with a 5\,s hop yields 45{,}064 windowed segments (NSR 26{,}898 / AF 3{,}942 / Other 14{,}224). This is the main benchmark.

\paragraph{MIT-BIH Arrhythmia Database~\cite{moody2001mitbih}.} 47 patient recordings at 360\,Hz with expert beat- and rhythm-level annotations. We assign each 10\,s window the dominant rhythm annotation and group rhythms into NSR / AF / Other consistent with the CinC label scheme. AF-bearing records (201, 202, 203, 210, 219, 221, 222) are distributed across train, val, and test (train: 202, 203, 219, 221; val: 201, 222; test: 210) to ensure no fold has zero positives. Total: 8{,}640 segments (NSR 6{,}300 / AF 790 / Other 1{,}550), split 7{,}020 / 540 / 540. MIT-BIH is used as a cross-dataset robustness check rather than the main benchmark; it is too small for meaningful UQ comparison (only 4 AF records in train) but is informative as a graceful-degradation control for the LW-CCSD method.

\subsection{Implementation}
All experiments use PyTorch 2.x; training runs on Apple Silicon MPS with CUDA portability validated. Synthesis caches per-record .npy pulse waveforms (~30\,MB total for CinC, ~10\,MB for MIT-BIH) so subsequent training epochs and evaluations skip the synthesis step. End-to-end reproducibility: the released repository contains all code, configurations, and trained checkpoints; the synthesis cache rebuilds from the public CinC and MIT-BIH downloads in approximately 20\,minutes of CPU time on a recent laptop. License: MIT. Repository: \url{https://github.com/ShahnawazKakarh/rppg-selective-arrhythmia}.

\subsection{Evaluation Protocol}
The validation set serves two distinct purposes that are kept methodologically separate: (a) early-stopping during classifier training (macro-F1), and (b) calibration / weight selection at evaluation time, for the conformal threshold $\hat{q}_\alpha$ and for the LW-CCSD weight vector $\mathbf{w}^{*}$. The test set is held out from all selection.

All selective-prediction metrics (AURC, selective accuracy at coverage, per-class recall) are computed on the test set with the operating point fixed by the validation procedure. The LW-CCSD constraint coverage is set to $c_0 = 0.50$ \emph{a priori} as the most adversarial setting --- per-class recall degrades fastest at low coverage --- and reported throughout. Sweeping the AF-recall floor at fixed $c_0$ yields the Pareto frontier reported in Section~V.

% =============================================================================
\section{Results}
% =============================================================================
\subsection{UQ Benchmark on Synthetic rPPG}
Table~\ref{tab:uq_comparison} compares the five uncertainty heads (plus the EDL ensemble configuration) on the CinC test set. The deterministic single-pass softmax serves as the no-UQ baseline; we additionally report a clean 5-member softmax-classifier ensemble (\emph{Clean Ensemble}) trained on the same data with different initialisation seeds.

\begin{table}[t]
\centering
\caption{UQ benchmark on synth-rPPG (CinC test, $n{=}6{,}750$). Best per column in \textbf{bold}.}
\label{tab:uq_comparison}
\begin{tabular}{lrrrr}
\toprule
Method & Acc & ECE $\downarrow$ & Brier $\downarrow$ & AURC $\downarrow$ \\
\midrule
Deterministic            & 0.686 & ---            & 0.440          & 0.2367 \\
MC Dropout (T{=}30)      & 0.715 & 0.076          & 0.418          & 0.1980 \\
Clean Ensemble (M{=}5)   & 0.692 & 0.052          & 0.440          & 0.2066 \\
SNGP                     & 0.729 & \textbf{0.029} & 0.393          & 0.2044 \\
EDL (KL10)               & 0.739 & 0.137          & 0.426          & 0.1725 \\
EDL Ensemble (M{=}5)     & \textbf{0.747} & 0.144 & \textbf{0.422} & \textbf{0.1640} \\
\bottomrule
\end{tabular}
\end{table}

Four findings.

\emph{(i)} The EDL ensemble (M{=}5) is the strongest configuration on aggregate AURC (0.1640) and accuracy (0.747), beating the single-pass EDL (0.1725, 0.739) by approximately one ensemble member's worth of inference cost. \emph{(ii)} SNGP wins calibration by a wide margin (ECE 0.029 vs the next-best 0.052 for Clean Ensemble) and is the most defensible choice when calibration is the deployment-critical metric. \emph{(iii)} The classical MC Dropout / Clean Ensemble methods sit between the deterministic baseline and the modern (EDL / SNGP) heads on AURC; the ranking is consistent with the broader UQ literature on small-to-medium classification benchmarks. \emph{(iv)} The deterministic baseline is the weakest on AURC (0.2367) and provides the largest \emph{absolute} headroom for LW-CCSD --- a finding we exploit in the deployment recommendation chain (Section~\ref{sec:cross_uq}).

\subsection{Na\"{i}ve SQI Deferral: An 18\,\% Gain That Is Clinically Harmful}
\label{sec:naive_sqi_failure}
We next combine model confidence with the spectral SNR feature via a single shared weight $w$ (the na\"{i}ve SQI rule in our taxonomy). Sweeping $w$ on the CinC test set yields a monotone improvement in aggregate AURC up to $w = 0.70$, where the AURC reaches $0.1942$ --- an $18.0\,\%$ relative improvement over the UQ-only baseline of $0.2367$. By the aggregate metric this is the largest single-feature gain we observe anywhere in this work.

The per-class breakdown in Table~\ref{tab:naive_sqi} reveals the cost.

\begin{table}[t]
\centering
\caption{Na\"{i}ve SQI deferral ($w = 0.70$). Aggregate AURC improves but AF recall collapses; numbers on CinC test set, $n{=}6{,}750$.}
\label{tab:naive_sqi}
\begin{tabular}{lrr}
\toprule
Coverage & UQ-only AF recall & Na\"{i}ve SQI AF recall \\
\midrule
0.50 & 0.707 & 0.043 \\
0.70 & 0.732 & 0.365 \\
0.80 & 0.764 & 0.637 \\
0.95 & 0.814 & 0.778 \\
1.00 & 0.828 & 0.828 \\
\bottomrule
\end{tabular}
\end{table}

At $50\,\%$ coverage, the kept subset has AF recall of $0.043$ vs $0.707$ under UQ-only --- an absolute drop of $66.4$ percentage points. Among the $559$ true AF segments in the test set, na\"{i}ve SQI deferral retains only $54$ at half coverage; the other $505$ are deferred. At $70\,\%$ coverage AF recall recovers somewhat to $0.365$ but remains far below UQ-only's $0.732$. AF recall only equalises with UQ-only near full coverage, by which point the deferral policy has no useful selectivity. Figure~\ref{fig:naive_sqi_failure} visualises the gap across the full coverage range and shows that both class-conditional fixes (AF-immune, LW-CCSD) preserve AF recall while still leveraging the SQI signal.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/naive_sqi_failure.pdf}
\caption{AF recall on the kept subset as coverage decreases, four policies on the CinC synth-rPPG test set. Na\"{i}ve SQI deferral collapses AF recall at low coverage; the class-conditional rules (AF-immune and LW-CCSD) preserve it.}
\label{fig:naive_sqi_failure}
\end{figure}

The mechanism is structural rather than accidental, and we analyse it in Section~\ref{sec:mechanism}. For now we note that the aggregate AURC improvement is real but misleading: it comes from preserving a high-SNR NSR subset that the classifier already classifies well and from deferring a low-SNR AF subset that the classifier could classify correctly were it not deferred. A clinical-screen deployment optimising aggregate AURC would silently miss the majority of AF cases; the naive metric does not surface this failure.

\subsection{LW-CCSD: The Pareto Frontier}
LW-CCSD addresses this by learning a per-predicted-class weight vector $\mathbf{w}^{*}$ on the validation set subject to an AF-recall floor. We fix the constraint coverage at $c_0 = 0.50$ and sweep the AF-recall floor; results on the CinC test set are in Table~\ref{tab:pareto}.

\begin{table}[t]
\centering
\caption{LW-CCSD Pareto frontier on synth-rPPG CinC test set (deterministic classifier, snr\_db, constraint coverage $0.50$).}
\label{tab:pareto}
\begin{tabular}{lcrrr}
\toprule
Val AF floor & $\mathbf{w}^{*}$ (NSR/AF/Other) & Test AURC & $\Delta$ vs UQ & AF cost \\
\midrule
0.60 & 0.0 / 0.1 / 0.5 & 0.2270 & +4.1\,\% & $-$0.004 \\
0.58 & 0.0 / 0.4 / 0.6 & 0.2082 & +12.1\,\% & $-$0.045 \\
0.55 & 0.3 / 0.4 / 0.4 & 0.1998 & +15.6\,\% & $-$0.081 \\
0.50 & 0.1 / 0.5 / 0.6 & 0.2020 & +14.7\,\% & $-$0.123 \\
0.45 & 0.2 / 0.5 / 0.5 & 0.1984 & +16.2\,\% & $-$0.166 \\
0.40 & 0.4 / 0.5 / 0.5 & 0.1958 & +17.3\,\% & $-$0.220 \\
\midrule
UQ-only (baseline) & --- & 0.2367 & --- & ~0.000 \\
Na\"{i}ve SQI ($w{=}0.7$) & shared & 0.1942 & +18.0\,\% & $-$0.664 \\
\bottomrule
\end{tabular}
\end{table}

At the strictest floor we evaluated ($0.60$ on validation, conservative against val UQ-only AF recall of $0.602$), the learned weight is essentially zero on the AF and Other classes and modest on NSR --- the optimiser is barely able to add any SQI weighting while respecting the constraint. The resulting test AURC is $0.2270$, a $4.1\,\%$ improvement over UQ-only, with AF recall almost perfectly preserved (delta $-0.004$). At the next-tighter floor of $0.58$ the optimiser opens up the AF and Other weights and recovers a $12.1\,\%$ AURC improvement at $4.5$ percentage-point AF cost. The frontier continues monotonically: $15.6\,\%$ AURC at $8.1$\,pp AF cost (floor $0.55$); $17.3\,\%$ AURC at $22.0$\,pp AF cost (floor $0.40$); and finally na\"{i}ve SQI ($18.0\,\%$ AURC at $66.4$\,pp AF cost) as the asymptote at zero safety.

A clinical operating point would be chosen by the deployer based on acceptable AF-recall harm. The $0.55$-floor configuration is the largest improvement that holds AF recall within $10$ percentage points of UQ-only and is our recommended default. The frontier shape is informative: na\"{i}ve SQI's $18\,\%$ aggregate gain is achievable only by surrendering essentially all AF recall, so the practical value of SQI augmentation is closer to $12$--$15\,\%$, not $18\,\%$. Figure~\ref{fig:pareto_frontier} plots the frontier in the (AF recall cost, test AURC) plane alongside the UQ-only and na\"{i}ve SQI reference points.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/pareto_frontier.pdf}
\caption{LW-CCSD Pareto frontier on synth-rPPG CinC. Each green circle is a learned operating point under a different val AF-recall floor (annotated). The blue square is the UQ-only baseline; the red cross is na\"{i}ve SQI deferral at $w{=}0.70$. The recommended $0.55$-floor operating point is circled.}
\label{fig:pareto_frontier}
\end{figure}

A small non-monotonicity (the floor-$0.55$ result has slightly better AURC than the floor-$0.50$ result, $0.1998$ vs $0.2020$) is a consequence of the discrete $11$-point grid; the global Pareto curve is smooth, but the discrete grid samples it at slightly different points. A continuous optimiser (discussed in Section~VI) would resolve this.

\subsection{Cross-UQ Replication and the Deployment Chain}
\label{sec:cross_uq}
We replicate the LW-CCSD evaluation across all five UQ sources, taking the strongest safe operating point ($\Delta \text{AF recall} \geq -0.05$) per source. Table~\ref{tab:cross_uq} reports the result.

\begin{table}[t]
\centering
\caption{LW-CCSD across UQ sources, CinC test set. Strongest operating point with AF-recall cost $\leq -0.05$ per source. EDL configurations show a \emph{negative} margin --- LW-CCSD does not help, see Section~\ref{sec:edl_mechanism}.}
\label{tab:cross_uq}
\begin{tabular}{lrrrr}
\toprule
UQ source & UQ-only AURC & LW-CCSD AURC & $\Delta$ rel & $\Delta$ AF recall \\
\midrule
Deterministic        & 0.2367 & \textbf{0.1998} & \textbf{+15.6\,\%} & $-$0.081 \\
MC Dropout (T{=}30)  & 0.1980 & 0.1899          & +4.1\,\%   & $-$0.029 \\
Clean Ensemble (M{=}5) & 0.2066 & 0.1928        & +6.7\,\%   & $-$0.027 \\
SNGP                 & 0.2044 & 0.1969          & +3.7\,\%   & $-$0.022 \\
EDL (KL10)           & 0.1725 & 0.1764          & $-$2.3\,\% & $-$0.013 \\
EDL Ensemble (M{=}5) & 0.1640 & 0.1679          & $-$2.4\,\% & $-$0.018 \\
\bottomrule
\end{tabular}
\end{table}

The pattern in the classical-UQ rows (Deterministic, MC Dropout, Clean Ensemble, SNGP) is consistent with the v1 paper finding: the SQI-deferral gain scales inversely with the underlying UQ informativeness. Deterministic has the weakest confidence ranking and gets the largest gain ($+15.6\,\%$); the modern UQ heads sit between $+3.7\,\%$ and $+6.7\,\%$. The EDL rows show the opposite sign --- the margin is negative ($-2.3\,\%$ for single EDL, $-2.4\,\%$ for EDL ensemble). LW-CCSD does \emph{not} help on top of EDL; it slightly hurts. We analyse this in Section~\ref{sec:edl_mechanism}.

\paragraph{The deployment chain.} Reading Table~\ref{tab:cross_uq} as a function of inference budget yields a clean deployment recommendation chain across four regimes:

\begin{itemize}
\item \emph{Smallest budget (single-pass inference, no UQ overhead):} Deterministic $+$ LW-CCSD $\to$ test AURC $0.1998$. The largest LW-CCSD margin lives here. A lightweight model with the free post-hoc rule.
\item \emph{Standard budget, AURC-first:} Single-pass EDL $\to$ test AURC $0.1725$, no LW-CCSD needed. The strongest AURC at single-pass inference; LW-CCSD is redundant here for the mechanism reason developed in Section~\ref{sec:edl_mechanism}.
\item \emph{Standard budget, calibration-first:} SNGP $+$ LW-CCSD $\to$ test AURC $0.1969$, ECE $0.029$. The strongest calibration in the whole study with a small AURC concession vs single-pass EDL.
\item \emph{Maximum AURC (M{=}5 inference budget):} EDL ensemble $\to$ test AURC $\mathbf{0.1640}$, accuracy $\mathbf{0.747}$, no LW-CCSD needed. The single strongest configuration in the benchmark.
\end{itemize}

A striking corollary of the chain: the deterministic-classifier LW-CCSD operating point at floor $0.55$ (test AURC $0.1998$) is \emph{below} the unaugmented Clean Ensemble UQ-only baseline of $0.2066$. A lightweight single-forward-pass classifier with the free post-hoc deferral rule is strictly better on the selective-prediction objective than a five-model softmax ensemble without deferral, at one-fifth the inference cost. This is the same conclusion as the v1 paper but stronger: LW-CCSD on a deterministic model now beats not only deep ensembles but also any other single-pass UQ method except EDL.

The MIT-BIH replication (supplementary; see \texttt{docs/baselines/lw\_ccsd\_v1/mitbih/}) returned the trivial solution $\mathbf{w}^{*} = (0, 0, 0)$, i.e.\ LW-CCSD reduced to UQ-only with no test-side change. The MIT-BIH classifier is degenerate (only $4$ AF training records, $90$--$100\,\%$ AF accuracy on test, $0\,\%$ NSR accuracy at low coverage); under such conditions the SQI signal has nothing orthogonal to add and the constrained optimiser correctly returns the no-op. This is the method's graceful-degradation property: when SQI cannot help, LW-CCSD does not hurt.

\subsection{Conformal Coverage Arc}
\label{sec:conformal_arc}
We instantiate the four conformal procedures from Section~\ref{sec:conformal_procedures} on the LW-CCSD deterministic-classifier substrate at family-wise $\alpha = 0.10$. Table~\ref{tab:conformal_arc} reports each procedure's best test AURC.

\begin{table}[t]
\centering
\caption{Conformal coverage arc: best LW-CCSD operating point per procedure on CinC (deterministic, family-wise $\alpha=0.10$).}
\label{tab:conformal_arc}
\begin{tabular}{lcrrr}
\toprule
Procedure & Floor & $\mathbf{w}^{*}$ & Test AURC & $\Delta$ rel \\
\midrule
UQ-only (no conformal)       & ---  & ---             & 0.2367 & --- \\
Per-class                    & 0.52 & 0.3 / 0.4 / 0.4 & 0.1998 & +15.6\,\% \\
Bonferroni joint             & 0.50 & 0.0 / 0.5 / 0.6 & 0.2046 & +13.55\,\% \\
Holm step-down joint         & 0.50 & 0.0 / 0.5 / 0.5 & 0.2033 & +14.11\,\% \\
MV-beta exact-independent    & 0.50 & 0.0 / 0.5 / 0.6 & 0.2046 & +13.55\,\% \\
MV-beta exact-independent    & 0.55 & 0.1 / 0.2 / 0.5 & 0.2160 & +8.75\,\% \\
\bottomrule
\end{tabular}
\end{table}

Three findings.

\emph{Per-class is the loosest constraint and gives the largest margin (+15.6\,\%).} It does not control family-wise coverage by construction, which is the price of the larger margin.

\emph{Bonferroni, Holm, and MV-beta all give family-wise $(1-\alpha)$ coverage and produce nearly identical results.} Bonferroni and MV-beta produce identical $\mathbf{w}^{*}$ and AURC at the operating-point grid resolution; the 0.0012 difference in their per-class $\alpha$ ($0.0345$ vs $0.0333$) is below the 0.10 grid step. Holm step-down recovers a $0.6\,\%$ rel AURC margin over Bonferroni by reordering the per-class p-value tests, but the gap is small. The remaining $1.75\,\%$ rel AURC gap between per-class and the joint procedures is therefore \emph{intrinsic to the joint-coverage objective}, not an artifact of Bonferroni's union-bound conservativeness; closing it further requires a different objective (e.g.\ relaxed coverage at lower $\alpha$) rather than a tighter joint bound.

\emph{MV-beta opens a new feasible operating point at floor 0.55.} The slightly more lenient per-class LCB makes a previously-infeasible higher-floor regime feasible (AURC $0.2160$, AF cost only $-0.025$). A deployer that wants stronger AF safety than the floor-0.50 procedures buy can take the MV-beta floor-0.55 result for a moderate AURC concession.

Figure~\ref{fig:lw_ccsd_margin_bar} visualises the conformal-arc Pareto and the cross-UQ LW-CCSD margin pattern side-by-side.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/lw_ccsd_margin_bar.pdf}
\caption{Left: paired UQ-only vs LW-CCSD-augmented test AURC across the six UQ configurations of Table~\ref{tab:uq_comparison}. Right: signed relative $\Delta$ AURC per UQ source. Green bars indicate LW-CCSD helps; red bars indicate it does not (the EDL configurations).}
\label{fig:lw_ccsd_margin_bar}
\end{figure}

\subsection{EDL Mechanism: In-Method Signal-Quality Absorption}
\label{sec:edl_mechanism}
The EDL rows in Table~\ref{tab:cross_uq} show a qualitatively different pattern from the classical UQ rows: the LW-CCSD margin is \emph{negative}. Adding the SNR-based deferral rule on top of an EDL classifier slightly hurts AURC rather than helping. This section establishes the finding at five independent levels of robustness and refines the mechanism to a controllable training-time consequence rather than an architectural property of the Dirichlet evidence head.

\paragraph{Level 1 --- Empirical optimisation.} On the canonical EDL configuration (KL annealing over 10 epochs, val macro-F1 best epoch), the LW-CCSD constrained optimiser at AF-recall floor $0.25$ converges to $\mathbf{w}^{*} = (0.0,\ 0.0,\ 0.4)$ with test AURC $0.1764$ vs single-pass-EDL AURC $0.1725$ ($\Delta = -2.3\,\%$). At every floor in the sweep ($0.20, 0.25, 0.28, 0.30$) the test AURC after LW-CCSD is greater than or equal to the EDL UQ-only baseline. The optimiser is selecting a small Other-class weight as a marginal hedge but does not find a configuration where SNR contributes positively. This is the basic observation that needs explaining: classical UQ heads admit a positive LW-CCSD margin; EDL does not.

\paragraph{Level 2 --- Variance across initialisation seeds (EDL ensemble M=5).}
\label{sec:edl_ensemble}
We train five EDL models with different initialisation seeds (the same procedure used for the Clean Ensemble of softmax classifiers in Section~\ref{sec:cross_uq}) and average the per-class predictive probabilities $\hat{p}_c$. The ensemble's test AURC is $\mathbf{0.1640}$ (test accuracy $\mathbf{0.747}$), the strongest configuration in the whole benchmark (Table~\ref{tab:uq_comparison}). LW-CCSD on the EDL ensemble converges to $\mathbf{w}^{*} = (0.0,\ 0.0,\ 0.4)$ and gives test AURC $0.1679$ ($\Delta = -2.4\,\%$). The per-seed variance of the LW-CCSD margin across the five ensemble members is $\sigma = 0.49\,\%$ (mean $-2.26\,\%$, range $[-1.68, -2.99]$). The negative-margin finding is therefore not a single-seed artefact; it is stable across initialisation.

\paragraph{Level 3 --- Conformal coverage at $\alpha = 0.10$.} We instantiate the four conformal procedures of Section~\ref{sec:conformal_procedures} on the EDL substrate at family-wise $\alpha = 0.10$. Per-class, Bonferroni, Holm, and MV-beta all return $\mathbf{w}^{*} = (0.0,\ 0.0,\ 0.4)$ with test AURC $0.1764$ ($\Delta = -2.3\,\%$). The finite-sample-coverage version of the LW-CCSD constraint does not surface a positive-margin operating point; the classical-UQ findings of Section~\ref{sec:conformal_arc} do not transfer to EDL.

\paragraph{Level 4 --- KL annealing schedule among annealed regimes.}
\label{sec:edl_kl_sensitivity}
We sweep the KL annealing length over $\{5, 10, 15, 25\}$ warm-up epochs at the same data and model seeds. Table~\ref{tab:edl_kl_sensitivity} reports the per-schedule test metrics and the LW-CCSD margin.

\begin{table}[t]
\centering
\caption{EDL KL annealing schedule sensitivity. The negative LW-CCSD margin holds across every annealed schedule.}
\label{tab:edl_kl_sensitivity}
\begin{tabular}{rrrrrr}
\toprule
KL & Test Acc & ECE & UQ-only AURC & LW-CCSD AURC & $\Delta$ rel \\
\midrule
5    & 0.7360 & 0.133          & 0.1680 & 0.1709 & $-$1.68\,\% \\
10   & 0.7390 & 0.138          & 0.1725 & 0.1764 & $-$2.28\,\% \\
15   & 0.7418 & 0.139          & 0.1713 & 0.1748 & $-$2.07\,\% \\
25   & 0.7341 & \textbf{0.124} & 0.1736 & 0.1788 & $-$2.99\,\% \\
\midrule
Mean $\pm$ std & --- & --- & 0.1714 & 0.1752 & $-$2.26 $\pm$ 0.49\,\% \\
\bottomrule
\end{tabular}
\end{table}

Across the four annealed schedules: UQ-only AURC sits in $[0.168, 0.174]$ (3.3\,\% relative spread across a 5$\times$ variation in annealing length); accuracy in $[0.734, 0.742]$; ECE in $[0.124, 0.139]$. The LW-CCSD margin is uniformly negative ($[-1.68, -2.99]\,\%$). EDL is therefore robust to the specific KL annealing schedule within the annealed regime: the no-LW-CCSD-margin property is not an accidental consequence of the canonical 10-epoch choice.

\paragraph{Level 5 --- KL$\to\infty$ ablation.} The natural sharpening question is what happens in the no-annealing limit. We train a fifth EDL configuration with $\lambda_t = 1$ from epoch 1 (no warm-up; the KL prior is applied at full weight throughout training). This is the KL$\to\infty$ ablation; it removes the warm-up phase that lets the evidence head develop selective collapse on noisy inputs before the prior tightens.

The result is qualitatively different from every annealed schedule. Test accuracy and ECE remain in the EDL band ($0.7382$, $0.133$); UQ-only AURC is $0.1718$, in-range. But the LW-CCSD margin \emph{disappears}: $\mathbf{w}^{*} = (0.0,\ 0.0,\ 0.3)$ at floor $0.25$ gives test AURC $0.1703$, a margin of $+0.92\,\%$ at zero AF cost (Table~\ref{tab:edl_klinf}). Without annealing the model still classifies competently but does not develop the input-quality-conditional evidence behaviour; LW-CCSD then offers a small positive margin like on classical UQ heads.

\begin{table}[t]
\centering
\caption{KL$\to\infty$ ablation (full KL prior from epoch 1) refutes the no-LW-CCSD-margin property in the no-annealing limit.}
\label{tab:edl_klinf}
\begin{tabular}{lrrrrr}
\toprule
KL & Test Acc & ECE & UQ-only AURC & LW-CCSD AURC & $\Delta$ rel \\
\midrule
Annealed (4-schedule mean) & 0.7377 & 0.134 & 0.1714 & 0.1752 & $-$2.26\,\% \\
$\to\infty$ (this ablation) & 0.7382 & 0.133 & 0.1718 & \textbf{0.1703} & $\mathbf{+0.92\,\%}$ \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:kl_annealing_sensitivity} visualises the KL annealing schedule and the KL$\to\infty$ refutation side-by-side.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/kl_annealing_sensitivity.pdf}
\caption{Left: EDL test AURC across KL annealing schedules and the KL$\to\infty$ ablation (no warm-up). Right: signed LW-CCSD relative $\Delta$ AURC per schedule. Every annealed schedule (KL $\in \{5, 10, 15, 25\}$) gives a negative margin; the no-annealing KL$\to\infty$ ablation (highlighted bar) gives a $+0.92\,\%$ margin --- the no-margin property is annealing-dependent.}
\label{fig:kl_annealing_sensitivity}
\end{figure}

\subsection{Refined Mechanism Statement}
The Section~\ref{sec:edl_mechanism} statement is therefore: \textbf{EDL with KL annealing absorbs signal-quality information in-method; EDL without annealing does not.} The no-LW-CCSD-margin property is a controllable training-time consequence of the KL warm-up schedule, not an architectural property of the Dirichlet evidence head. The warm-up phase lets the evidence head develop selective collapse on noisy inputs --- low evidence for all classes on hard windows, high evidence on confident-classifiable windows --- which is the in-method realisation of the external SNR signal LW-CCSD would otherwise re-introduce. Once the collapse develops, the SNR ranking is redundant with the EDL uncertainty ranking; mixing them at $w > 0$ marginally degrades the combined ranking.

Four deployment implications follow.
\emph{(i)} A deployer who wants LW-CCSD to add value on top of EDL should disable the KL annealing schedule. Trade-off: $\sim 0.4\,\%$ rel AURC vs the best annealed schedule (KL=5 at $0.1680$ vs KL$\to\infty$ at $0.1718$) for a small positive LW-CCSD margin.
\emph{(ii)} A deployer who wants the cheapest deferral-free selective behaviour should keep annealing on. Single-pass EDL (KL=10) is the AURC-first standard choice in the deployment chain of Section~\ref{sec:cross_uq}.
\emph{(iii)} The annealed default is the most defensible choice for the paper. The KL$\to\infty$ ablation establishes that the mechanism is controllable; it does not make the annealed default obsolete.
\emph{(iv)} The mechanism statement is now \emph{falsifiable} at the substrate level: a future real-data substrate that shows a positive LW-CCSD margin on KL10-trained EDL would refute it. Substrate-level falsifiability is a stronger property than substrate-level confirmation.

The full mechanism robustness story spans five independent levels: empirical optimisation, variance across initialisation seeds ($\sigma = 0.49\,\%$), conformal coverage at family-wise $\alpha = 0.10$, KL annealing schedule among annealed regimes, and the KL$\to\infty$ ablation that refutes the property in the no-annealing limit. We are aware of no plausible internal-EDL-framework null hypothesis remaining. The open question is whether the property survives on the real face-video AF substrate; that is the v2.0 critical experiment.

\subsection{Mechanism: Why Na\"{i}ve SQI Fails on AF}
\label{sec:mechanism}
The per-class collapse in Table~\ref{tab:naive_sqi} is a direct consequence of how spectral SNR responds to the rhythm structure that defines AF. SNR as we compute it is the ratio of cardiac-band power ($0.7$--$3.0$\,Hz) to out-of-band power. NSR concentrates this band power at a single fundamental frequency and its harmonics, producing a sharp spectral peak and high SNR. AF, by definition, has irregular beat intervals; in the frequency domain this disperses power across the cardiac band into a broad ridge with no dominant peak, producing a substantially lower SNR even when the underlying pulse waveform itself is clean and free of motion noise.

Empirically on our CinC test set, the median cardiac-band SNR is $23.7$\,dB for true NSR segments, $20.0$\,dB for true Other segments, and $12.5$\,dB for true AF segments --- an $11.3$\,dB shift between NSR and AF that is intrinsic to their rhythm structure rather than to recording quality. Figure~\ref{fig:snr_distribution} shows the full per-class SNR distributions and confirms that the AF distribution sits cleanly to the left of the NSR distribution with substantial overlap that no model recalibration can resolve. A deferral rule that ranks samples by SNR alone therefore systematically promotes NSR over AF and demotes AF over NSR, independently of the model's predictions. When this SNR ranking is mixed with the model's confidence ranking at high weight ($w = 0.70$), the SNR shift dominates and AF predictions are pushed below the deferral threshold even when the model is confident on them.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/snr_distribution.pdf}
\caption{Per-class cardiac-band SNR distributions on the CinC synth-rPPG test set. The $11.3$\,dB median shift between NSR ($23.7$\,dB) and AF ($12.5$\,dB) is intrinsic to rhythm structure --- it is the mechanism behind the AF-recall collapse of na\"{i}ve SQI deferral.}
\label{fig:snr_distribution}
\end{figure}

A second consequence is that the failure cannot be fixed by recalibrating the model. The model could in principle learn to flag AF predictions on low-SNR segments with lower confidence, partially absorbing the SNR signal into the confidence ranking. But it cannot resolve the inverse case --- a confident AF prediction on a low-SNR window that is genuinely AF --- because the SNR is low \emph{because the rhythm is AF}. There is no rescaling of confidence that recovers AF from a deferral rule that penalises low SNR.

This is what LW-CCSD discovers automatically. Across the Pareto frontier the learned weight on the AF class is consistently smaller than the weights on NSR and Other: at the recommended floor of $0.55$, $\mathbf{w}^{*}_{\text{AF}} = 0.4$ vs $\mathbf{w}^{*}_{\text{NSR}} = 0.3$, $\mathbf{w}^{*}_{\text{Other}} = 0.4$; at the most safety-conservative floor of $0.60$, $\mathbf{w}^{*}_{\text{AF}}$ drops to $0.1$ while the others stay positive. The optimiser is performing a class-conditional credit assignment: the SNR feature is reliable for ranking NSR / Other predictions but unreliable for ranking AF predictions, and the learned weights reflect that.

% =============================================================================
\section{Discussion}
% =============================================================================
\subsection{Why the Method Works}
LW-CCSD succeeds because spectral SNR carries deferral-relevant information that model confidence does not, but only for some predicted classes. Predictions of NSR or Other rhythms on noisy windows are typically over-confident: the classifier learned features tied to rhythm regularity, which are corrupted by camera motion, lighting flicker, and partial face occlusion in ways the per-window confidence score cannot detect. SNR exposes this corruption directly. Predictions of AF, on the other hand, are correctly less confident on noisy windows (because the AF-defining irregularity raises uncertainty in the same direction as motion noise) but \emph{also} less confident on clean windows (because regularity is the NSR signature). Weighting SNR on AF predictions therefore double-counts the irregularity penalty and rejects positives. The learned per-class weight resolves this: the optimiser discovers a small or zero weight on the AF class and larger weights on NSR / Other, precisely where SNR adds orthogonal information.

\subsection{Deployment Implications}
LW-CCSD applied to a single-pass deterministic classifier achieves test AURC $0.1998$ at AF-recall cost $-0.081$ (operating point: val floor $0.55$). An unaugmented five-model deep ensemble on the same data achieves test AURC $0.2155$ --- worse, at five times the inference compute. The lightweight model plus the free post-hoc deferral rule outperforms ensembling. For clinical-screen deployment where compute and latency budgets are tight, this suggests LW-CCSD is a viable substitute for ensembling rather than an add-on to it.

The cross-UQ pattern also has a practical implication: when investing in stronger UQ (MC Dropout at $T = 30$, ensembles at $M = 5$), the marginal value of adding SNR-based deferral shrinks. A deployed system should therefore make the UQ-method choice and the SQI-deferral choice jointly --- they are partial substitutes, not independent additions.

\subsection{Limitations}
The main benchmark uses synthetic rPPG derived from ECG, not real face-video rPPG. The synthesis preserves rhythm structure (which the classifier ultimately relies on for AF detection) but abstracts away morphological details that real rPPG would also lack. We expect the qualitative trends to transfer to real face-video AF data, particularly the central failure mode of na\"{i}ve SQI deferral on AF, which depends only on the spectral concentration of cardiac-band energy and not on synthesis details. Quantitative magnitudes (the exact size of the +15.6\,\% deterministic AURC gain, the exact slope of the Pareto frontier) may shift on real data. Replication on OBF~\cite{li2018obf} or MAHNOB-HCI is the natural next step pending data access.

The LW-CCSD optimiser uses a discrete 11-point grid. Generalisation noise from grid discreteness manifests as small non-monotonicities in the test-side Pareto curve (e.g.\ floor 0.55 occasionally beating floor 0.50 by 0.002 AURC). A continuous gradient-free optimiser (Nelder--Mead, CMA-ES) on val AURC subject to constraint penalties would smooth this and is a natural refinement.

The AF-recall floor used in the optimisation is an empirical val-set quantity. A conformal formulation, in which the floor is replaced by a finite-sample distribution-free coverage guarantee under exchangeability, would strengthen the theoretical claim. This is a focused next experiment, not a fundamental redesign --- the LW-CCSD optimiser already operates on rank-normalised scores, which are exchangeability-friendly.

\subsection{Generality of the Pattern}
The core failure mode --- aggregate-metric improvement that comes from systematically rejecting the minority class because the class-defining signal collides with the auxiliary deferral feature --- is not specific to rPPG-AF screening. It appears whenever the auxiliary feature is correlated with class identity in ways the aggregate metric does not capture. The fix --- learn per-class weights subject to per-class recall constraints --- is correspondingly general. LW-CCSD as formulated here applies directly to any classification task with a continuous auxiliary quality feature and a per-class safety floor, and is a candidate methodology for the broader literature on selective prediction in clinical AI.

% =============================================================================
\section{Conclusion}
% =============================================================================
We introduced LW-CCSD, a post-hoc class-conditional deferral procedure that learns per-predicted-class signal-quality weights subject to a configurable safety floor. On a 45{,}064-segment synthetic-rPPG benchmark, LW-CCSD recovers most of the aggregate AURC gain of na\"{i}ve signal-quality deferral while preserving AF recall, and applied to a lightweight single-pass classifier it outperforms an unaugmented five-model deep ensemble. The method is model-agnostic, requires no retraining, and generalises across three uncertainty quantification methods. We release the full pipeline and CinC-derived synth-rPPG substrate under MIT licence; replication on real face-video AF data is a natural next step pending OBF / MAHNOB-HCI access.

% =============================================================================
\section*{Acknowledgments}
% =============================================================================
This work uses public datasets --- PhysioNet/CinC 2017 AF Challenge and the MIT-BIH Arrhythmia Database --- whose maintainers are gratefully acknowledged.

% =============================================================================
\section*{Data and Code Availability}
% =============================================================================
Open-source pipeline including all scripts, configurations, trained checkpoints, and per-method evaluation artefacts is available at \url{https://github.com/ShahnawazKakarh/rppg-selective-arrhythmia} under MIT licence. The CinC-derived synthetic-rPPG cache is reproducible from the released code in approximately 20 minutes of CPU time on a recent laptop.

% =============================================================================
\section*{Conflict of Interest Statement}
% =============================================================================
The author declares no competing interests.

% =============================================================================
\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
